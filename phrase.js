export default class Phrase {
    static phrase
    static length
    static coeff

    constructor() {
        this.phrase = "Je suis ton pere"
        this.length = this.phrase.length
        this.coeff = this.length
    }
}